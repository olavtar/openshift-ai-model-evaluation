# Default values for model-evaluation
global:
  imageRegistry: quay.io
  imageRepository: rh-ai-quickstart
  imageTag: latest
  imagePullPolicy: Always
  storageClass: ""

# Model serving configuration
models:
  # MaaS endpoint (RHOAI 3.x default - zero GPU required)
  maasEndpoint: "https://maas.apps.prod.rhoai.rh-aiservices-bu.com"
  # Default models for A/B comparison
  modelA:
    name: "granite-3.1-8b-instruct"
    deploymentMode: "maas"
  modelB:
    name: "llama-3.1-8b-instruct"
    deploymentMode: "maas"
  # Embedding model for RAG pipeline
  embeddingModel: "nomic-embed-text-v1.5"

# Secrets configuration - override with actual secrets
secrets:
  # Database secrets
  POSTGRES_DB: "model-evaluation"
  POSTGRES_USER: "user"
  POSTGRES_PASSWORD: "changeme"

  # API secrets
  DATABASE_URL: "postgresql+asyncpg://user:changeme@model-evaluation-db:5432/model-evaluation"
  DEBUG: "false"
  ALLOWED_HOSTS: '["*"]'

  # Model API tokens (MaaS authentication)
  MODEL_API_TOKEN: ""

  # UI secrets
  VITE_API_BASE_URL: ""
  VITE_ENVIRONMENT: "production"

# OpenShift Routes configuration
routes:
  enabled: true
  annotations:
    haproxy.router.openshift.io/timeout: "600s"
  sharedHost: ""
  ui:
    host: ""
    tls:
      enabled: true
      termination: edge
      insecureEdgeTerminationPolicy: Redirect
  api:
    host: ""
    tls:
      enabled: true
      termination: edge
      insecureEdgeTerminationPolicy: Redirect

# API configuration
api:
  enabled: true
  name: model-evaluation-api
  image:
    repository: model-evaluation-api
    tag: latest
  service:
    type: ClusterIP
    port: 8000
  replicas: 1
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"
  healthCheck:
    enabled: true
    liveness:
      path: /health/live
      initialDelaySeconds: 10
      periodSeconds: 10
    readiness:
      path: /health/ready
      initialDelaySeconds: 15
      periodSeconds: 10

# UI configuration
ui:
  enabled: true
  name: model-evaluation-ui
  image:
    repository: model-evaluation-ui
    tag: latest
  service:
    type: ClusterIP
    port: 8080
  replicas: 1
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "500m"
  healthCheck:
    enabled: true
    path: /
    initialDelaySeconds: 10
    periodSeconds: 10

# Database configuration (self-managed pgvector)
database:
  enabled: true
  name: model-evaluation-db
  image:
    repository: pgvector/pgvector
    tag: pg16
  service:
    type: ClusterIP
    port: 5432
  persistence:
    enabled: true
    size: 10Gi
    accessMode: ReadWriteOnce
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

# Database migration configuration
migration:
  enabled: true
  name: model-evaluation-migration
  image:
    repository: model-evaluation-api
    tag: latest
  restartPolicy: Never
  backoffLimit: 1
  ttlSecondsAfterFinished: 86400
  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"

# Subchart: pgvector (from ai-architecture-charts)
pgvector:
  enabled: false

# Subchart: llm-service (from ai-architecture-charts)
llm-service:
  enabled: false

# ServiceAccount
serviceAccount:
  create: true
  annotations: {}
  name: ""

# Pod Security Context
podSecurityContext: {}

# Security Context
securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault

# Node selector
nodeSelector: {}

# Tolerations
tolerations: []

# Affinity
affinity: {}
